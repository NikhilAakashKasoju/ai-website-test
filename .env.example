# Copy to .env.local for local dev, or set as an env var in Render's dashboard.
# Points the contact form at your PHP backend once it's deployed.
# Leave unset and the form will show "Form endpoint not connected yet." instead of failing silently.
NEXT_PUBLIC_CONTACT_ENDPOINT=
